/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DayScheduleService } from "../daySchedule.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DayScheduleCreateInput } from "./DayScheduleCreateInput";
import { DayScheduleWhereInput } from "./DayScheduleWhereInput";
import { DayScheduleWhereUniqueInput } from "./DayScheduleWhereUniqueInput";
import { DayScheduleFindManyArgs } from "./DayScheduleFindManyArgs";
import { DayScheduleUpdateInput } from "./DayScheduleUpdateInput";
import { DaySchedule } from "./DaySchedule";
import { ScheduleIntervalFindManyArgs } from "../../scheduleInterval/base/ScheduleIntervalFindManyArgs";
import { ScheduleInterval } from "../../scheduleInterval/base/ScheduleInterval";
import { ScheduleIntervalWhereUniqueInput } from "../../scheduleInterval/base/ScheduleIntervalWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DayScheduleControllerBase {
  constructor(
    protected readonly service: DayScheduleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DaySchedule })
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: DayScheduleCreateInput
  ): Promise<DaySchedule> {
    return await this.service.create({
      data: {
        ...data,

        status: data.status
          ? {
              connect: data.status,
            }
          : undefined,

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        date: true,
        id: true,
        note: true,

        status: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DaySchedule] })
  @ApiNestedQuery(DayScheduleFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<DaySchedule[]> {
    const args = plainToClass(DayScheduleFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        date: true,
        id: true,
        note: true,

        status: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DaySchedule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: DayScheduleWhereUniqueInput
  ): Promise<DaySchedule | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        date: true,
        id: true,
        note: true,

        status: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DaySchedule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: DayScheduleWhereUniqueInput,
    @common.Body() data: DayScheduleUpdateInput
  ): Promise<DaySchedule | null> {
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          status: data.status
            ? {
                connect: data.status,
              }
            : undefined,

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          date: true,
          id: true,
          note: true,

          status: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DaySchedule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: DayScheduleWhereUniqueInput
  ): Promise<DaySchedule | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          date: true,
          id: true,
          note: true,

          status: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/scheduleIntervals")
  @ApiNestedQuery(ScheduleIntervalFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ScheduleInterval",
    action: "read",
    possession: "any",
  })
  async findManyScheduleIntervals(
    @common.Req() request: Request,
    @common.Param() params: DayScheduleWhereUniqueInput
  ): Promise<ScheduleInterval[]> {
    const query = plainToClass(ScheduleIntervalFindManyArgs, request.query);
    const results = await this.service.findScheduleIntervals(params.id, {
      ...query,
      select: {
        category: true,
        createdAt: true,

        daySchedule: {
          select: {
            id: true,
          },
        },

        end: true,
        id: true,
        label: true,
        start: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/scheduleIntervals")
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "update",
    possession: "any",
  })
  async connectScheduleIntervals(
    @common.Param() params: DayScheduleWhereUniqueInput,
    @common.Body() body: ScheduleIntervalWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      scheduleIntervals: {
        connect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/scheduleIntervals")
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "update",
    possession: "any",
  })
  async updateScheduleIntervals(
    @common.Param() params: DayScheduleWhereUniqueInput,
    @common.Body() body: ScheduleIntervalWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      scheduleIntervals: {
        set: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/scheduleIntervals")
  @nestAccessControl.UseRoles({
    resource: "DaySchedule",
    action: "update",
    possession: "any",
  })
  async disconnectScheduleIntervals(
    @common.Param() params: DayScheduleWhereUniqueInput,
    @common.Body() body: ScheduleIntervalWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      scheduleIntervals: {
        disconnect: body,
      },
    };
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
